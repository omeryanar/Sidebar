<UserControl x:Class="Sidebar.Module.Clock.ClockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource MaterialDesignPaper}" Loaded="{DXEvent 'Load()'}" Unloaded="{DXEvent 'Unload()'}">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
        <Label Content="{Binding CurrentTime}" FontSize="72" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="35,20,0,0"
               FontFamily="/Sidebar.Common;component/Assets/Fonts/#DS-Digital" Foreground="{DynamicResource PrimaryHueMidBrush}" />
        <Label Content="{Binding CurrentDate}" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left" Padding="35,0" />
    </StackPanel>
    
</UserControl>
